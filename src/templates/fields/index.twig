{% extends "_layouts/cp" %}

{% block contextMenu %}
    {% set showSiteMenu = (craft.app.getIsMultiSite() ? true : false) %}
    {% if showSiteMenu %}
        {% include "_elements/sitemenu" %}
    {% endif %}
{% endblock %}

{% set title = "Entry relations manager"|t('entryrelationsmanager') %}

{% set content %}
    {% block blockContent %}
        <div class="elements">
            <div class="tableview">
                <table class="data fullwidth collapsible entryrelationsmanager">
                </table>
            </div>
        </div>
    {% endblock %}

    <hr>

    <div id="plugin-footer">
        <div class="footer-left">
            {% block footerButton %}

            {% endblock %}
        </div>

        <div class="footer-right">
            <a class="plugin-credit" href="https://frontwise.com" target="_blank">
                <span class="credit">Made by Frontwise</span>
            </a>
        </div>
    </div>
{% endset %}